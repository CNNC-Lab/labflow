# simulation-jax template

Minimal JAX-based simulation scaffold. Includes:
- dataclass config with seed + physical parameters
- PRNG key management (split, not globals)
- Placeholder for diffrax integration

Replace EXPERIMENT_NAME with the actual name when `labflow new` scaffolds it.
