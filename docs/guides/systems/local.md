# Local workstation

Default system. No queueing; runs in-process. Env defaults to `nest-dev` conda env per `HOWTO-NEST-dev.md`.

```bash
labflow run my_experiment  # --system defaults to local-workstation
```
